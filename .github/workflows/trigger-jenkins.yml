name: Trigger Jenkins Build Action

on:
  pull_request:
    branches:
      - master
    types: [closed]

jobs:
  build:
    name: Test Build
    runs-on: ubuntu-latest
    if: github.event.pull_request.merged == true
    
    steps:
    - name: Trigger SampleJob
      uses: appleboy/jenkins-action@master
      with:
        url: "http://54.221.6.255:8080/job/SampleJob/"
        user: "admin"
        token: ${{ secrets.TOKEN }}
        job: "foobar"
